# Core framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Pydantic / settings
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9   # PostgreSQL (Render)

# Computer vision
opencv-python-headless==4.8.1.78
Pillow==10.1.0
numpy==1.24.3

# ML (TensorFlow optional — only required if model files are present)
# tensorflow==2.15.0  # Uncomment when deploying with model files

# Audio analysis
librosa==0.10.1
soundfile==0.12.1

# Auth utilities (kept for token generation helpers)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Logging
structlog==23.2.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
